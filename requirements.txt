# Python dependencies for Netlify deployment

# For build scripts and utilities
requests>=2.28.1
pyyaml>=6.0

# Don't list Jekyll dependencies here
# Jekyll and its plugins are managed through Gemfile
# See Gemfile for Ruby dependencies 